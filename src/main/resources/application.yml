spring:
  application:
    name: TastyBytes
  datasource:
    url: jdbc:mysql://localhost:3306/tasty_bytes
    username: root
    password: system
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

server:
  port: 8086

mail:
  host: smtp.gmail.com
  port: 587
  username: paridasonu2001@gmail.com
  password: vdos qprs tjjs cxpn
  properties:
    mail:
      smtp:
        auth: true
        starttls:
          enable: true
